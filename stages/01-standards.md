# Stage 1 shared standard

A report describes exactly one run. Preserve the supplied event log. A tool result
alone cannot establish a completed case. Test with temporary shop state; keep the
baseline workbench runnable. No credentials or external requests in this exercise.
