#!/bin/sh
set -eu
cd "$(dirname "$0")/.."
if [ ! -x .venv-agent/bin/python ]; then
  echo 'Run scripts/setup-agent for the pinned schema-validator environment.' >&2
  exit 2
fi
exec .venv-agent/bin/python -m unittest exercises.extraction_orchestration.checks -v
