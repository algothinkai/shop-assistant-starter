#!/bin/sh
set -eu
cd "$(dirname "$0")/.."
if [ ! -x .venv-agent/bin/python ]; then
  echo 'Run scripts/setup-agent first. This check needs the optional pinned Agent SDK.' >&2
  exit 2
fi
exec .venv-agent/bin/python -m unittest exercises.agent_hooks.checks -v
