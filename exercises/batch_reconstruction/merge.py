"""Reconstruct chunk proposals without accepting a document automatically."""


def reconstruct(manifest, original_results, chunks, retry_results, *, evidence_mode):
    raise NotImplementedError(
        "Bind chunks, preserve field evidence and revalidate whole source"
    )
